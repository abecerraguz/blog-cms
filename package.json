{
  "name": "cms-blog",
  "version": "1.0.0",
  "private": true,
  "description": "CMS Blog — Frontend (Vite + React) + Backend (Express)",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,yellow \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend && cd backend && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}